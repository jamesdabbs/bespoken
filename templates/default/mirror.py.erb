#!/usr/bin/python

import json
from subprocess import call

dirs = json.loads('<%= node["mirror"]["directories"].to_json %>')
_to  = "<%= node['mirror']['to'] %>"

for _from in dirs:
  call(["rsync", "-avz", _from, _to])

